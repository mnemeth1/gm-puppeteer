node_modules/
dist/
coverage/
*.log
.env
.env.local
.env.*.local

# editor / OS
.DS_Store
.vscode/
.idea/
*.swp

# puppeteer user data dirs — the default FORGE_PROFILE_DIR for Forge mode.
# Holds a live Foundry/Forge session; never commit it. A custom
# FORGE_PROFILE_DIR must be added here (or kept outside the repo) too.
.puppeteer-profile/

# Debug artifacts (screenshots, DOM dumps, etc.)
debug-output/

# Local Claude Code MCP project-scope config (dev convenience)
.mcp.json

# Local dev smoke-test harness for the browser login flow
scripts/smoke-login.mjs
